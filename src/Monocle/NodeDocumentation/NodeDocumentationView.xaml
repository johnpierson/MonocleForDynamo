<Window x:Class="MonocleViewExtension.NodeDocumentation.NodeDocumentationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MonocleViewExtension.NodeDocumentation"
             mc:Ignorable="d" 
             Height="400"
             Width="400">
    <Window.Resources>
        <FontFamily x:Key="ComicNeueu">pack://application:,,,/NodeDocumentation/#ComicNeu</FontFamily>
        <Style TargetType="{x:Type Window}">
            <Setter Property="FontFamily" Value="{StaticResource ComicNeueu}" />
        </Style>
    </Window.Resources>
    <Grid Margin="8" Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="48"/>
        </Grid.RowDefinitions>
        <Label Content="Mega Ultra Node Documenter" FontSize="24"></Label>

        <StackPanel Grid.Row="1">
            <Label Content="Base Path:" FontSize="12"></Label>
            <TextBox Name="BasePath" VerticalContentAlignment="Center" Text="{Binding Path, Mode=TwoWay, FallbackValue=C:/}" Margin="4 4  0 0" Padding="8 0 0 0"/>

            <Label Content="Full Node Name:" FontSize="12"></Label>
            <TextBox Name="FullNodeName" VerticalContentAlignment="Center" Text="{Binding FullNodeName, Mode=TwoWay, FallbackValue=NodeName}" Margin="4 4  0 0" Padding="8 0 0 0"/>


            <Label Content="Node Name:" FontSize="12"></Label>
            <TextBox Name="NodeName" VerticalContentAlignment="Center" Text="{Binding NodeName, Mode=TwoWay, FallbackValue=NodeName}" Margin="4 4  0 0" Padding="8 0 0 0"/>

            <Label Content="Brief Description:" FontSize="12"></Label>
            <TextBox Name="BriefDescription" VerticalContentAlignment="Center" Text="{Binding Description, Mode=TwoWay, FallbackValue=Brief}" Margin="4 4  0 0" Padding="8 0 0 0"/>

            <Label Content="Not So Brief Description:" FontSize="12"></Label>
            <TextBox Name="ExtendedDescription" Height="64"  Text="{Binding ExtendedDescription, Mode=TwoWay, FallbackValue=Brief}" Margin="4 4  0 0" Padding="8 0 0 0"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Visible"/>

        </StackPanel>

<Grid Grid.Row="2">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Content="Get Node" Command="{Binding GetNodeCommand}"/>
        <Button Grid.Column="2" Content="Document" Command="{Binding CreateDocumentation}"/>

</Grid>

</Grid>
    </Grid>
</Window>
